---
import Layout from '../layouts/Layout.astro'

import { profile } from '@/settings'
import { publications } from '@/data/cv'
import ProfilePictures from '@/assets/index.png'
import Hero from '@/components/ui/Hero.astro'
import Grid from '@/components/ui/Grid.astro'
import ArticleList from '@/components/ui/ArticleList.astro'

const { fullName, title, institute, research_areas } = profile
---

<Layout>
	<!-- Hero Section -->
	<section class='flex items-center gap-12 border-b pb-12'>
		<Hero
			fullName={fullName}
			title={title}
			institute={institute}
			profilePicture={ProfilePictures}
		/>
	</section>

	<!-- Domaines d'expertise -->
	<section class='py-12 border-b'>
		<Grid gridTitle="Domaines d'expertise" gridItems={research_areas} />
	</section>

	<!-- Réalisations récentes -->
	<section class='py-12 border-t'>
		<ArticleList listTitle='Réalisations récentes' listItems={publications} />
	</section>
</Layout>
